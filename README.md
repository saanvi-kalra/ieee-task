# ieee-task
Name: Saanvi Kalra 

Task Description:   Familiarisation with Lists, loops, arrays (numpy), dataframes (pandas) and csv analysis, and graphs (matplotlib) in Python. Basics of GitHub: repository creation, commits. 
* Note: signicance of last graph in q6: Several low-scorers have shown improvement, whereas high-scorers performed poorly in the final test.

Set-Up Instructions: 
Python installation: Version compatible with device from python.orgs/downloads/ 
Numpy installation: on terminal, run: pip3 install numpy
Likewise for pandas and matplotlib

Commands:  
List methods such as .copy(), .append(), .remove(), and .sort()
In loops: Break and for-else
In numpy: np.array, shape, dtype, vectorized operations, mean, min, max, std, and Boolean indexing.
In Pandas: pd.read_csv(), DataFrames, columns, filtering, creating columns, sorting, and CSV
input/output
In matplotlib: plt.bar(), plt.scatter(), plt.hist(), labels, titles, readability, and saving figures
